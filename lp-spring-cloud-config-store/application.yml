eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    
project:
  service :
    base :
      uri : http://projectservice/project/
task:
  service :
    base :
      uri : http://taskservice/project/
