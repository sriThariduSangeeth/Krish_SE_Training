server:
  port: 8595
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sriThariduSangeeth/LPTraining
          search-paths:
            - 'lp-spring-cloud-config-store/*service'
  application:
    name: config-server
